server:
  port: 7000

springdoc:
  swagger-ui.path: /openapi/swagger-ui.html
  api-docs.path: /openapi/v3/api-docs
  packagesToScan: net.sanchezapps.tasksservice
  pathsToMatch: /**

api:
  common:
    version: 1.0.0
    title: Sample API
    description: Description of the API...

spring:
  application:
    name: tasks-service

  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@test_high?TNS_ADMIN=ATP_Wallet
    username: TASKS_DB
    password: VUeXKEsXEnr8cZW
  jpa:
    database: ORACLE
    hibernate:
      ddl-auto: update
    show-sql: true


---
server:
  port: 80

spring:
  application:
    name: tasks-service
  config:
    activate:
      on-profile: docker